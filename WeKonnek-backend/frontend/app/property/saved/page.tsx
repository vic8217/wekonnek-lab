'use client';
import { useEffect, useState } from 'react';import { useRouter } from 'next/navigation';import PropertyCard from '@/components/PropertyCard';import { getToken } from '@/hooks/use-auth';import { propertyApi, type SavedPropertyRow } from '@/lib/property';
export default function SavedProperties(){const router=useRouter(),[rows,setRows]=useState<SavedPropertyRow[]>([]);useEffect(()=>{if(!getToken()){router.replace('/auth/login?redirect=%2Fproperty%2Fsaved');return;}propertyApi.saved().then(setRows);},[router]);return <div className="mx-auto max-w-7xl px-4 py-8"><h1 className="text-3xl font-black">My Saved Properties</h1><div className="mt-6 grid gap-5 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4">{rows.map(row=><PropertyCard key={row.id} listing={row.listing}/>)}</div>{!rows.length&&<p className="py-20 text-center text-slate-500">Properties you save will appear here.</p>}</div>}
