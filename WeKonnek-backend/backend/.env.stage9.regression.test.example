# Stage 9 current-schema regression database (do not commit secrets)
# Copy to .env.stage9.regression.test.
#
# Required:
#   DATABASE_URL pointing at database wekonnek_stage9_regression_test
#
# Do NOT reuse historical acceptance DBs (stage0–stage8).

DATABASE_URL=postgresql://wekonnek_stage9_regression_test:CHANGE_ME@127.0.0.1:5432/wekonnek_stage9_regression_test
JWT_SECRET=dev-secret
