# Stage 5B dedicated test database (do not commit secrets)
# Copy to .env.stage5b.test and fill credentials.
#
# Required:
#   DATABASE_URL pointing at database wekonnek_stage5b_test
#
# Do NOT reuse wekonnek_stage5_test / stage4 / stage3 / stage2 / stage1 / stage0.

DATABASE_URL=postgresql://wekonnek_stage5b_test:CHANGE_ME@127.0.0.1:5432/wekonnek_stage5b_test
JWT_SECRET=dev-secret
