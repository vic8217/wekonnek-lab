# Stage 6 dedicated test database (do not commit secrets)
# Copy to .env.stage6.test and fill credentials.
#
# Required:
#   DATABASE_URL pointing at database wekonnek_stage6_test
#
# Do NOT reuse wekonnek_stage5b_test / stage5 / stage4 / stage3 / stage2 / stage1 / stage0.

DATABASE_URL=postgresql://wekonnek_stage6_test:CHANGE_ME@127.0.0.1:5432/wekonnek_stage6_test
JWT_SECRET=dev-secret
